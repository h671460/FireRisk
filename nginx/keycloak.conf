# # HTTP: allow ACME challenge + redirect everything else to HTTPS
# server {
#   listen 80;
#   server_name auth.keycloak-fireguard-group-02.com;

#   location /.well-known/acme-challenge/ {
#     root /var/www/certbot;
#   }

#   location / {
#     return 301 https://$host$request_uri;
#   }
# }

# # HTTPS: Keycloak reverse proxy
# server {
#   listen 443 ssl;
#   server_name auth.keycloak-fireguard-group-02.com;

#   ssl_certificate     /etc/letsencrypt/live/auth.keycloak-fireguard-group-02.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/auth.keycloak-fireguard-group-02.com/privkey.pem;

#   location / {
#     proxy_pass http://keycloak:8080;
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Proto https;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Host $host;
#     proxy_set_header X-Forwarded-Port 443;
#   }
# }
server {
  listen 80;
  server_name auth.keycloak-fireguard-group-02.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 200 "OK\n";
  }
}